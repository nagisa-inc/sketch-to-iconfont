{
  "name": "sketch-to-iconfont",
  "version": "0.0.1",
  "description": "",
  "main": "gulpfile.babel.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run generate",
    "gulp": "gulp",
    "generate": "npm run gulp -- export:svg && bundle exec fontcustom compile && rimraf ./src/svg/*"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nagisa-inc/sketch-to-iconfont.git"
  },
  "author": "nagisa-inc",
  "devDependencies": {
    "gulp": "^3.9.1",
    "gulp-sketch": "^1.0.5",
    "rimraf": "^2.6.2",
    "run-sequence": "^2.2.1"
  },
  "dependencies": {}
}
